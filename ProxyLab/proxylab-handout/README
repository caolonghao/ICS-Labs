####################################################################
# CS:APP Proxy Lab
#
# Student Source Files
#
####################################################################

This directory contains the files you will need for the CS:APP Proxy
Lab.

proxy.c
csapp.h
csapp.c
    These are starter files.  csapp.c and csapp.h are described in
    your textbook.

    You may make any changes you like to these files.  And you may
    create and handin any additional files you like.

Makefile
    This is the makefile that builds the proxy program.  Type "make"
    to build your solution, or "make clean" followed by "make" for a
    fresh build. 

    Type "make handin" to create the tarfile that you will be handing
    in. You can modify it any way you like. Autolab will use your
    Makefile to build your proxy from source.

driver.sh
    The autograder for Basic, Concurrency, Cache and Real Pages.
    usage: ./driver.sh

webdriver_test.py
    The script to run Real Pages test. Remember to run your proxy 
    and tiny web server first.
    usage: ./webdriver_test.py <proxy_port> <tiny_port>

nop-server.py
     Helper for the autograder.         

tiny
    Tiny Web server from the CS:APP text. It is slightly modified to
    support features required by Real Pages tests.
    usage: cd tiny && ./tiny <port>

